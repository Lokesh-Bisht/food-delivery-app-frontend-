import { styled } from "@mui/system";

export const LogoContent = styled("div")({
  minWidth: "fitContent",
  alignItem: "ceneter",
  display: "flex",
  alignItems: "center",
});

export const LogoImage = styled("img")({
  width: "1.5rem",
  marginRight: "0.5rem",
});
